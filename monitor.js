/**
 * PayOps Email Monitor — Claude Code Routine
 * Schedule: Every 1 hour, 9:00 AM to 6:30 PM UAE, Monday to Friday
 */

const { ROUTINE_PROMPT } = require('./Duplicates and returned');

module.exports = { prompt: ROUTINE_PROMPT };
